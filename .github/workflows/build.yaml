name: Build

on:
  workflow_dispatch:
  push:
    tags:
      - "v*.*.*" # Trigger on version tags like v1.0.0

jobs:
  test:
    name: Test
    uses: ./.github/workflows/test.yaml

  snap:
    name: Snap
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: snapcore/action-build@v1
        id: snapcraft
      - uses: actions/upload-artifact@v4
        with:
          name: snap
          path: ${{ steps.snapcraft.outputs.snap }}

  build:
    name: Build
    needs: test
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            runner: ubuntu-24.04
            binary: ripping
            binary-name: ripping-x86_64-unknown-linux-gnu
          - target: aarch64-unknown-linux-gnu
            runner: ubuntu-24.04-arm
            binary: ripping
            binary-name: ripping-aarch64-unknown-linux-gnu
          - target: x86_64-apple-darwin
            runner: macos-latest
            binary: ripping
            binary-name: ripping-x86_64-apple-darwin
          - target: aarch64-apple-darwin
            runner: macos-latest
            binary: ripping
            binary-name: ripping-aarch64-apple-darwin
          - target: x86_64-pc-windows-msvc
            runner: windows-latest
            binary: ripping.exe
            binary-name: ripping-x86_64-pc-windows-msvc.exe
          - target: aarch64-pc-windows-msvc
            runner: windows-latest
            binary: ripping.exe
            binary-name: ripping-aarch64-pc-windows-msvc.exe
    steps:
      - uses: actions/checkout@v4

      - name: Install target
        run: rustup target add ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Rename binary
        shell: bash
        run: mv target/${{ matrix.target }}/release/${{ matrix.binary }} target/${{ matrix.target }}/release/${{ matrix.binary-name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.target }}
          path: target/${{ matrix.target }}/release/${{ matrix.binary-name }}

  release:
    name: Release
    needs: [build, snap]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          files: dist/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
